<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hyweb.phone.gip"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/icon_about"
        android:label="@string/app_name"
         >
        <activity
            android:name=".IndexPage"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainTabActivity" android:launchMode="singleTask"></activity>
        <activity android:name=".MainTabContent" android:launchMode="singleTask"></activity>
        <activity android:name=".SegmentPage" android:launchMode="singleTask"></activity>
        <activity android:name=".ListPage"  android:launchMode="singleTask"></activity>
    </application>

</manifest>